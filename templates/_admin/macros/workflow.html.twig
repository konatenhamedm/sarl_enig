
{% macro workflow(workflow) %}

    <div class="row row-line mb-4">
        <div class="col-1 ">
            {# <label class="sr-onl" for="userinput1"> <h3>Nom du document</h3></label>#}
            {{ form_widget(workflow.numeroEtape) }}
        </div>
        <div class="col-5">
            {# <label class="sr-onl" for="userinput1"> <h3>Nom du document</h3></label>#}
            {{ form_widget(workflow.libelleEtape) }}
        </div>
        <div class="col-1">
            {# <label class="sr-onl" for="userinput1"> <h3>Nom du document</h3></label>#}
            {{ form_widget(workflow.nombreJours) }}
        </div>
        <div class="col-2">
            {# <label class="sr-onl" for="userinput1"> <h3>Nom du document</h3></label>#}
            {{ form_widget(workflow.propriete) }}
        </div>
        <div class="col-2">
            {{ form_widget(workflow.route) }}
        </div>
        <div class="col-1  del-col">
        </div>
    </div>

{% endmacro %}

{% macro image(image) %}
{#{{ dump(image) }}#}
    <div class="row ligne row-line">
        <div class="col-md-3">
            {{ form_row(image.numeroEtape,{'attr':{'class':'form-control numero-etape numero','id':'userinput16','placeholder':'Numéro étape','readonly':'readonly'}}) }}
        </div>
        <div class="col-md-6 ">
            {{ form_row(image.libelleEtape,{'attr':{'class':'form-control ','id':'userinput16','placeholder':'Libelle étape'}}) }}
        </div>
        <div class="col-md-2 ">
            {{ form_row(image.nombreJours,{'attr':{'class':'form-control ','id':'userinput16','placeholder':'Nombre de jours'}}) }}
        </div>
      {#  <div class="col-md-3 ">
            {{ form_row(image.typeActe,{'attr':{'class':'form-control motif','id':'userinput16','placeholder':'Type acte'}}) }}
        </div>#}

        <div class="col-md-1  del-col supprimer">

        </div>

    </div>

{% endmacro image %}